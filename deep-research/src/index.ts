import { definePluginEntry } from "openclaw/plugin-sdk/plugin-entry";

export default definePluginEntry({
  id: "deep-research",
  name: "Deep Research",
  description: "Guide multi-step sourced web research inside OpenClaw using the existing web search stack",
  register() {},
});
