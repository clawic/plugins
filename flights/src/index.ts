import { definePluginEntry } from "openclaw/plugin-sdk/plugin-entry";

export default definePluginEntry({
  id: "flights",
  name: "Flights",
  description: "Guide flight research inside OpenClaw using the existing web search stack",
  register() {},
});
